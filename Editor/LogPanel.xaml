<Window x:Class="Editor.LogPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Editor"
        xmlns:diag="clr-namespace:Engine.Diagnostics;assembly=Engine"
        Title="Log Viewer" Height="600" Width="1000"
        Background="#1E1E1E">
    <Window.Resources>
        <local:LogLevelToColorConverter x:Key="LogLevelToColor"/>
        
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>
    
    <DockPanel>
        <!-- Toolbar -->
        <Border DockPanel.Dock="Top" Background="#2D2D30" Padding="8" BorderBrush="#3F3F46" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Filter Level -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,16,0">
                    <TextBlock Text="Min Level:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox SelectedItem="{Binding FilterLevel}" Width="100">
                        <diag:LogLevel>Debug</diag:LogLevel>
                        <diag:LogLevel>Info</diag:LogLevel>
                        <diag:LogLevel>Warn</diag:LogLevel>
                        <diag:LogLevel>Error</diag:LogLevel>
                    </ComboBox>
                </StackPanel>
                
                <!-- Search Box -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Width="250" 
                             Background="#252526" 
                             Foreground="White" 
                             BorderBrush="#3F3F46"
                             Padding="4"/>
                </StackPanel>
                
                <!-- Action Buttons -->
                <Button Grid.Column="2" Content="Clear" Click="ClearButton_Click" 
                        Margin="8,0" Padding="12,4" Background="#0E639C" Foreground="White" BorderThickness="0"/>
                <Button Grid.Column="3" Content="Close" Click="CloseButton_Click" 
                        Padding="12,4" Background="#3F3F46" Foreground="White" BorderThickness="0"/>
            </Grid>
        </Border>
        
        <!-- Log Entries List -->
        <ListView ItemsSource="{Binding Entries}" 
                  Background="#1E1E1E" 
                  BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#3F3F46" BorderThickness="0,0,0,1" Padding="8,4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" 
                                       Text="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" 
                                       Foreground="#808080"
                                       FontFamily="Consolas"/>
                            
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding Level}" 
                                       Foreground="{Binding Level, Converter={StaticResource LogLevelToColor}}"
                                       FontWeight="Bold"
                                       FontFamily="Consolas"/>
                            
                            <TextBlock Grid.Column="2" 
                                       Text="{Binding Source}" 
                                       Foreground="#569CD6"
                                       FontFamily="Consolas"/>
                            
                            <TextBlock Grid.Column="3" 
                                       Text="{Binding Message}" 
                                       TextWrapping="Wrap"
                                       Foreground="White"
                                       FontFamily="Consolas"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</Window>
